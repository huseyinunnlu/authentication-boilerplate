export interface IGenerateRandomParams {
    length?: number
    onlyLetters?: boolean
    onlyNumbers?: boolean
    isOnlyUpperCase?: boolean
    isOnlyLowerCase?: boolean
    includeSpecialChars?: boolean
    customDigits?: string
}